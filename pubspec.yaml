name: tinseltown_iq
description: "Tinseltown IQ - AI-powered movie and TV concept validation"
publish_to: 'none'
version: 1.0.2+3

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  provider: 6.1.5+1
  shared_preferences: 2.5.3
  flutter_stripe: 11.4.0
  http: 1.3.0
  google_sign_in: ^6.2.2
  sign_in_with_apple: ^6.1.4
  crypto: ^3.0.6
  connectivity_plus: ^6.1.4
  pdf: ^3.11.1
  printing: ^5.13.4
  path_provider: ^2.1.5
  flutter_local_notifications: ^18.0.1
  # In-App Purchase for iOS subscriptions (Apple requirement)
  in_app_purchase: ^3.2.0
  # Firebase packages (LOCKED versions for stability)
  firebase_core: 3.6.0
  cloud_firestore: 5.4.3
  firebase_auth: 5.3.1
  firebase_messaging: 15.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
